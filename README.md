# V2VApp

Native Kotlin Android app for V2V telemetry exchange + trajectory prediction.

## Model asset
The model file is intentionally not committed because it exceeds GitHub's regular file size limit.

Place your model at:

`app/src/main/assets/vehicle_trajectory.tflite`

Or configure Git LFS before committing model assets.
